'use client';
import { useEffect, useRef } from 'react';
import { useStore, useStoreApi } from 'xBuilder/store';

export default function UpdateContent({ properties }: { properties: any }) {
  return null;
}
